import ContactForm from "@/components/ContactForm";

export const metadata = {
  title: "contact page",
  description: "",
};

const Contact = () => {
  return (
      <ContactForm/>
  );
};

export default Contact;
